# Root-level overrides for docker-compose. The actual values live in
# backend/.env (copied from backend/.env.example). This file just documents
# the keys the api container looks for.

APP_ENV=dev
LOG_LEVEL=INFO

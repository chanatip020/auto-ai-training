# Optional. Defaults to "" which routes everything through Vite's /api proxy
# (configured in vite.config.ts to point at http://localhost:8000).
# In production, set this to the public origin of your API, e.g.:
#   VITE_API_BASE=https://api.example.com
VITE_API_BASE=
